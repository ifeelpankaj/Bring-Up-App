/**
 * Font name constants — single source of truth.
 * Import FONTS wherever you need a fontFamily value.
 *
 * These keys must exactly match the keys passed to useFonts() in _layout.tsx.
 */
export const FONTS = {
  cameliya: "Cameliya",
  moralana: "Moralana",
  rockybilly: "Rockybilly",
  rushDriver: "RushDriver",
  sprintura: "Sprintura",
} as const;
